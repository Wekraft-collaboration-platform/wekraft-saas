import React from "react";

const TeamPage = () => {
  return (
    <div>
      {/* at top 3 boxes 
owner - name + avatsar !
admin - name + avatr (upto 3 )
members - name + avatr - upto 3 

skip  viewer !

then team memebr display -> all member avatar / name and role  in a table format with actions to chnage role and remove (but only owner and admin can perform such actions)

charts - > workload balance, top contributoers,  Deadline Accuracy Score .
!! */}
    </div>
  );
};

export default TeamPage;
